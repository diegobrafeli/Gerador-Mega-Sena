import { ScreenButton } from "../components/ScreenButton";

export default function Counter(){
    return(
        <div style={{display:"flex" , height: "100vh", alignItems: "center", justifyContent: "center", background:"#b6b4b4" }}>
            <ScreenButton />
        </div>
    )
}